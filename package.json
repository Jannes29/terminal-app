{
  "name": "terminal-app-collector",
  "version": "1.0.0",
  "private": true,
  "description": "Sammelt 24/7 echte Macro-Snapshots in history.json (terminal-app Repo)",
  "scripts": {
    "collect": "node collect.js"
  },
  "dependencies": {
    "playwright": "1.44.0"
  }
}
